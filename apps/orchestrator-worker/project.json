{
  "name": "orchestrator-worker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/orchestrator-worker/src",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "options": {
        "outputPath": "dist/apps/orchestrator-worker",
        "main": "apps/orchestrator-worker/src/main.ts",
        "tsConfig": "apps/orchestrator-worker/tsconfig.json",
        "rootDir": "."
      }
    },
    "typecheck": {
      "command": "tsc --noEmit -p apps/orchestrator-worker/tsconfig.json"
    }
  }
}
