apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ai-sdlc.fullname" . }}-api
  labels:
    {{- include "ai-sdlc.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  replicas: {{ .Values.api.replicas }}
  selector:
    matchLabels:
      {{- include "ai-sdlc.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        {{- include "ai-sdlc.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: api
    spec:
      serviceAccountName: {{ include "ai-sdlc.fullname" . }}
      containers:
        - name: api
          image: "{{ .Values.api.image.repository }}:{{ .Values.api.image.tag }}"
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - configMapRef:
                name: {{ include "ai-sdlc.fullname" . }}-config
            - secretRef:
                name: {{ include "ai-sdlc.fullname" . }}-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ai-sdlc.fullname" . }}-worker
  labels:
    {{- include "ai-sdlc.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  replicas: {{ .Values.worker.replicas }}
  selector:
    matchLabels:
      {{- include "ai-sdlc.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        {{- include "ai-sdlc.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker
    spec:
      serviceAccountName: {{ include "ai-sdlc.fullname" . }}
      containers:
        - name: worker
          image: "{{ .Values.worker.image.repository }}:{{ .Values.worker.image.tag }}"
          envFrom:
            - configMapRef:
                name: {{ include "ai-sdlc.fullname" . }}-config
            - secretRef:
                name: {{ include "ai-sdlc.fullname" . }}-secrets
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ai-sdlc.fullname" . }}-credential-proxy
  labels:
    {{- include "ai-sdlc.labels" . | nindent 4 }}
    app.kubernetes.io/component: credential-proxy
spec:
  replicas: {{ .Values.credentialProxy.replicas | default 2 }}
  selector:
    matchLabels:
      {{- include "ai-sdlc.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: credential-proxy
  template:
    metadata:
      labels:
        {{- include "ai-sdlc.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: credential-proxy
    spec:
      serviceAccountName: {{ include "ai-sdlc.fullname" . }}
      containers:
        - name: credential-proxy
          image: "{{ .Values.credentialProxy.image.repository | default .Values.api.image.repository }}:{{ .Values.credentialProxy.image.tag | default .Values.api.image.tag }}"
          ports:
            - containerPort: 4000
              name: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
