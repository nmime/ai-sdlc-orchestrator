{{- if .Values.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "ai-sdlc.fullname" . }}
  labels:
    {{- include "ai-sdlc.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "ai-sdlc.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
{{- end }}
