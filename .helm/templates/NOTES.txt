AI SDLC Orchestrator has been deployed!

Components:
  - API Server: {{ include "ai-sdlc.fullname" . }}-api (port 3000)
  - Worker: {{ include "ai-sdlc.fullname" . }}-worker
  - Credential Proxy: {{ include "ai-sdlc.fullname" . }}-credential-proxy (port 4000)
  - Dashboard: {{ include "ai-sdlc.fullname" . }}-dashboard (port 80)

{{- if .Values.ingress.enabled }}
Access the application at:
  https://{{ .Values.ingress.host }}
{{- else }}
To access the dashboard, run:
  kubectl port-forward svc/{{ include "ai-sdlc.fullname" . }}-dashboard 8080:80
  Then open http://localhost:8080

To access the API, run:
  kubectl port-forward svc/{{ include "ai-sdlc.fullname" . }}-api 3000:3000
{{- end }}

Ensure the following external services are configured:
  - PostgreSQL: {{ .Values.postgresql.host }}:{{ .Values.postgresql.port }}
  - Temporal: {{ .Values.temporal.address }}
  - Redis: {{ .Values.redis.url }}
  - MinIO: {{ .Values.minio.endpoint }}
